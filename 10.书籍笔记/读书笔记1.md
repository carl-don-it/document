# 如何分析多线程

用一条线程做不动，然后另一条线程的某行代码在前一行代码上下某个地方（从上下一行到非常远的距离都有可能，不可知的）执行，然后分析是否成功同步。

关键的两行代码都是操作和判断某个共享变量，以此做状态改变。

1. abs
    	1. head的next节点（第一个等待节点）不断自旋，其他等待节点也可以自旋，但是不可以tryAcquire。
         	1. node的waitStatus是signal，如果 `if (h != null && h.waitStatus != 0)`在 `compareAndSetWaitStatus`前，那么不需要unpark，直接拿到同步状态。如果在后面，那么有可能park，有可能不park，但都保证成功
            	2. 线程的同步通信很像循环，条件检测，改变条件，进行逻辑，下一步。这些组合起来

# 深入理解操作系统

chapter4、5、6、7跳读不明白

# corejava

https://horstmann.com/corejava/

# 计算机架构

编码讲了如何造cpu，内存，硬盘和主板的一些东西。说了计算机语言，还有与之对应的汇编。这些机器如何执行代码。

然后体系结构说了更多关于汇编，和cpu和c语言上的东西。差不多是嵌入式。

然后导论就说了建立在汇编之上的操作系统接管了底层硬件，封装了汇编，抽象出进程，为了更方便管理内存抽象出内存管理（虚拟内存管理并不是必须的，而是进化而来的），最后就是接管了io，暴露出系统调用和函数库供上层软件使用。。

